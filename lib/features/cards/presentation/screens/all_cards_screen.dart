// All Cards Screen
import 'package:flutter/material.dart';

class AllCardsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('All Cards')),
      body: Center(child: Text('Display all cards here')),
    );
  }
}
